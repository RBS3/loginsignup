
name: Rana Security Pipeline
on: [push]
jobs:
  security_scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Pipeline Status
        run: echo "🚀 Rana Security Pipeline is active on $GITHUB_REPOSITORY"
